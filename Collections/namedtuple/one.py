# Create a 2D point as tuple
point = (2, 3)

#print the coordinate (x, y)
print(point)
# Access coordinate (x)
print(point[0])
# Access coordinate (y)
print(point[1])

# Try to update coordinate value

point[0] = 3